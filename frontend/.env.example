# Set this to your Function App URL after `terraform apply`.
# The CI pipeline reads `terraform output -raw function_app_url` and injects it
# here before running `npm run build`, so the SPA bundles the correct endpoint.
#
# Example:
REACT_APP_API_URL=https://func-empportal-dev-abcd.azurewebsites.net/api
