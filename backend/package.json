{
  "name": "employee-portal-api",
  "version": "1.0.0",
  "description": "Azure Functions v4 API — employee portal",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "build:production": "npm run build",
    "watch": "tsc --watch",
    "prestart": "npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/cosmos": "^4.1.0",
    "@azure/functions": "^4.5.0",
    "@azure/identity": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
